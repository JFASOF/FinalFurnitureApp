import 'package:flutter/material.dart';

class FurnitureNotFound extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("404"),
    );
  }
}